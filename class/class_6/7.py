
# 7.编写函数，传入函数中多个实参
#     （均为可迭代对象，如字符串、元祖、列表、集合等），
#     将每个实参的每个元素依次加入到函数的动态参数args里面，
#     例如传入两个参数[1, 2, 3] (10, 20）最终args为（1,2,3,10,20)


def merge(*lists) -> tuple:
    return lists


s1 = [1, 2, 3]
s2 = (10, 20)
print(merge(*s1, *s2))
